name: "lint"
on:
  pull_request:
  workflow_dispatch:

jobs:
  # linting
  linting:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v3
      with:
        node-version: 12
    - run: npm ci
    - run: npm run lint
